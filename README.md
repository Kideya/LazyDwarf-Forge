# LazyDwarf-Forge
LazyDwarf Forge is a modding data generator for Noble Fates that creates complete, ready-to-use mod packages.  It generates a structured ZIP archive containing everything needed for a mod, so you don’t have to manually assemble files or folders.
